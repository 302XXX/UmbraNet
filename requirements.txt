# UmbraNet runtime dependencies (Python 3.10–3.13, Windows x64)
# Install: python -m pip install -r requirements.txt
# Update direct pins and constraints.txt together, then run Linux/Windows CI.
-c constraints.txt

# GUI
PySide6==6.9.3

# DNS / networking
dnslib==0.9.26
requests==2.32.5
psutil==7.1.0

# Encrypted DNS
aioquic==1.3.0
pynacl==1.6.0

# Release version comparison (PEP 440, including dev/rc versions)
packaging==25.0
