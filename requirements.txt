# UmbraNet - dependencies
# Install:  pip install -r requirements.txt

# GUI
PySide6>=6.7.0

# Core DNS / networking
dnslib>=0.9.24
requests>=2.28.0
psutil>=5.9.0

# Encrypted DNS transports
aioquic>=1.0.0      # DoQ (DNS-over-QUIC)
pynacl>=1.5.0       # DNSCrypt

# Optional DPI engine (Windows / WinDivert)
# pydivert>=2.1.0
